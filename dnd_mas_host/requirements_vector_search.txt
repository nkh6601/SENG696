# MongoDB Vector Search Requirements
# Install with: pip install -r requirements_vector_search.txt

# IMPORTANT: For GPU support, install PyTorch FIRST with:
#   pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu130
# Then run: pip install -r requirements_vector_search.txt

# Core dependencies
pymongo>=4.6.0              # MongoDB driver
sentence-transformers>=3.0.0 # Local embedding models (newer version with better compat)
transformers>=4.30.0        # Transformers library
numpy>=1.24.0               # Array operations

# PyTorch (will be skipped if already installed with GPU support)
torch>=2.6.0                # PyTorch - install GPU version separately if needed
torchvision>=0.19.0         # Vision utilities

# Additional dependencies
pillow>=9.0.0               # Image processing
tqdm>=4.65.0                # Progress bars
